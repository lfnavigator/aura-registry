# Source and governance note

The user clarified the actual validation logic:

- FEM computations were initially validated against real machine / experimental data, including the mono-crystal cutting machine context.
- The validated FEM route was then used to calibrate/correct the improved perturbation method.
- AURA implements the perturbation-method route, so PM-vs-FEM rows are meaningful only as part of this chain.

Therefore the public record must not be reduced to a single direct-experiment row. The record must expose the chain structure.

Primary-source locator still to add for top-tier publication:

- exact dissertation section/table/figure for the real-machine/FEM validation basis;
- exact Excel/table locator for the FEM-vs-experiment P05 row if available;
- V. Breshev final expert signoff.
